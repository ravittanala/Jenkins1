pipeline {
  agent any
  stages {
    stage('Build') {
      agent any
      steps {
        sh '''echo "Hello World"

'''
        sh '''echo "Multi line shell steps works too"
ls -lah'''
      }
    }
  }
}